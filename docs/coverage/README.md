# Coverage Evidence

The final Gate submission will retain:

- a Cobertura XML report;
- a readable single-file HTML report; and
- the exact generation command and measured backend line percentage.

No coverage result exists during the foundation-only phase. Generated EF Core migrations may be
excluded, but application code will not be excluded merely to inflate the result.
